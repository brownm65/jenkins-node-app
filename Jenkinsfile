pipeline {
    agent any

    stages {
        stage('Simple Build Proof') {
            steps {
                sh 'echo "Jenkins successfully pulled the GitHub repo."'
                sh 'ls -la'
                sh 'cat package.json'
            }
        }
    }
}